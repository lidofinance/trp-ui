import styled from 'styled-components';
import { WalletCard } from '@lidofinance/ui-primitives';

export const WalletCardStyled = styled(WalletCard)`
  background: linear-gradient(52.01deg, #1b3349 0%, #01a2ff 100%);
`;
